{
  "name": "todo-electron-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "npx concurrently \"vite\" \"npx wait-on tcp:5173 && electron .\"",
    "electron:build": "vite build && electron ."
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1",
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "electron": "^38.1.2",
    "vite": "^7.1.7",
    "wait-on": "^9.0.1"
  }
}
